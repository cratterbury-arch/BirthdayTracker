<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".BirthdayApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Birthday Tracker"
        android:supportsRtl="true"
        android:theme="@style/Theme.BirthdayTracker">

        <!-- Main App -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.BirthdayTracker.Splash"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget Settings Screen -->
        <activity
            android:name=".WidgetSettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.BirthdayTracker" />

        <!-- Widget Provider -->
        <receiver
            android:name=".BirthdayWidgetProvider"
            android:exported="true">

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/birthday_widget_provider" />
        </receiver>

    </application>

</manifest>