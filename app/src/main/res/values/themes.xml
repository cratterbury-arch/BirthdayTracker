<resources>

    <!-- Main App Theme -->
    <style name="Theme.BirthdayTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Keep empty if you're using Compose theme system -->
    </style>

    <!-- Splash Theme -->
    <style name="Theme.BirthdayTracker.Splash" parent="Theme.SplashScreen">

        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen</item>

        <item name="postSplashScreenTheme">@style/Theme.BirthdayTracker</item>

    </style>

</resources>
